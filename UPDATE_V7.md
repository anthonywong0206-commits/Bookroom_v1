# v7 更新：日曆預約管理＋房間時長流程

## 1. 管理員「借用狀況」日曆
- 點擊日期可直接新增預約。
- 已有預約可在日曆下方檢視、修改及刪除。
- 新增／修改房間預約時，可一併設定同日借用物品及數量。
- 房間日曆會在主預約下顯示「同日物品」。
- 同一件物品亦會同步出現在其物品日曆；批准房間預約時，關聯物品會一併批准。
- 刪除房間主預約時，關聯物品紀錄會一併刪除。
- 原有綠色／紅色／灰色日曆及封鎖日期功能繼續保留。

## 2. 前台房間預約流程
新流程：
1. 選擇房間
2. 選擇日期
3. 選擇預約時間：半小時／1 小時／2 小時／其他
4. 選擇開始時間
5. 選擇是否需要同日借用物品

開始時間會以 30 分鐘為間隔，並按照管理員設定的可用時段、封鎖日期及已批准預約自動計算。

「其他」可自訂借用分鐘數（30 分鐘為一個單位）。

## 3. 房間／物品確認資料
房間及外借物品確認頁均只收集：
- 申請者姓名
- 電話
- 用途
- 備註

用途直接以按鈕顯示，並與管理員「用途設定」同步。

## 4. Supabase 正式模式
如 `config.js` 使用：

```js
demoMode: false
```

更新網站檔案後，請執行：

```text
supabase/20260909_v7_calendar_room_duration.sql
```

Migration 新增：
- 公開安全的資源忙碌時段 RPC（不包含姓名／電話／用途）
- 房間任意時長的公開預約驗證
- 管理員日曆新增／修改預約 RPC
- 房間與關聯物品同步
- 房間群組審批／完成狀態同步
- 刪除房間預約時同步刪除關聯物品

## 5. Demo Mode
Demo Mode 不需執行 SQL。前後台仍共用 `rrbs_admin_demo_v4` localStorage，新增／修改／刪除及房間關聯物品均會同步。
