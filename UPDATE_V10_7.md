# v10.7 更新：房間／物品預約管理重整

## 管理員後台
- 「房間／物品」改為「房間／物品設定」，只管理名稱、簡介、圖片、容量／庫存、分類及啟用狀態。
- 移除基本資料頁內的時段管理。
- 原「借用狀況」拆分為：
  - 房間預約管理
  - 物品預約管理
- 每頁上方以卡片顯示已建立的房間／物品。
- 保留月曆設計：綠＝空位、紅＝已借用、灰＝不可借用。
- 新增「批量新增開放時段」：可一次勾選星期一至日並設定開始／結束時間。
- 點擊日曆日期後，以每小時顯示當日狀態，管理員可將個別小時設為可借用／不可借用。
- 已有批准／完成預約的時段不可直接封鎖。

## 前台
- 「查詢」頁在選擇資源及日期後，以每小時時間表顯示可借用狀況。
- 綠＝可借用、紅＝已借用、灰＝不可借用。
- 房間實際可選時間仍按預約時長、已借用時段及管理員設定計算。

## Supabase
- 新增 `resource_time_overrides`。
- 新增管理員 RPC `admin_set_resource_time_override(...)`。
- `get_organization_portal_data(...)` 會安全回傳所屬機構的時間覆寫設定。
- 公開提交若撞正明確封鎖時段，資料庫會拒絕，避免只靠前端限制。

LIVE Supabase 已於 2026-09-22 套用本版本 migration。
